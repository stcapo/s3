# 🔧 缺失功能实现指南

## 📌 概述

本文档提供了所有缺失功能的详细实现指南，包括代码示例、API设计和前端组件结构。

---

## 🔴 P0 优先级功能

### 1. 忘记密码功能

#### 后端 API 设计

**端点 1: 请求密码重置**
```
POST /api/auth/forgot-password
请求体:
{
  "email": "user@example.com"
}
响应:
{
  "success": true,
  "message": "重置链接已发送到邮箱"
}
```

**端点 2: 重置密码**
```
POST /api/auth/reset-password
请求体:
{
  "token": "reset_token_xxx",
  "newPassword": "newpass123"
}
响应:
{
  "success": true,
  "message": "密码重置成功"
}
```

#### 前端页面结构

**ForgotPassword.tsx**
```typescript
- 邮箱输入框
- 发送重置链接按钮
- 成功提示信息
- 返回登录链接
```

**ResetPassword.tsx**
```typescript
- 新密码输入框
- 确认密码输入框
- 重置密码按钮
- 密码强度指示器
```

#### 数据库更新
```json
{
  "users": [
    {
      "id": "user-1",
      "email": "user@example.com",
      "resetToken": "token_xxx",
      "resetTokenExpiry": 1702857600000
    }
  ]
}
```

---

### 2. 活动创建/编辑功能

#### 后端 API 设计

**端点 1: 创建活动**
```
POST /api/events
请求体:
{
  "name": "演唱会名称",
  "description": "描述",
  "date": "2025-12-25",
  "location": "北京",
  "price": 299,
  "totalSeats": 1000,
  "seatMap": "url_to_seat_map"
}
响应:
{
  "id": "event-new",
  "name": "演唱会名称",
  ...
}
```

**端点 2: 编辑活动**
```
PUT /api/events/:id
请求体: 同创建
响应: 更新后的活动信息
```

#### 前端页面结构

**EventCreate.tsx**
```typescript
- 活动名称输入
- 活动描述编辑器
- 日期选择器
- 地点选择
- 票价输入
- 座位数输入
- 座位图上传
- 提交按钮
```

**EventEdit.tsx**
```typescript
- 预填充现有数据
- 编辑表单
- 保存按钮
- 取消按钮
```

#### 数据库更新
```json
{
  "events": [
    {
      "id": "event-1",
      "name": "演唱会",
      "organizerId": "user-1",
      "createdAt": 1702857600000,
      "updatedAt": 1702857600000
    }
  ]
}
```

---

### 3. 支付方式选择

#### 后端 API 设计

**端点: 获取支付方式**
```
GET /api/payment-methods
响应:
{
  "methods": [
    {
      "id": "alipay",
      "name": "支付宝",
      "icon": "url"
    },
    {
      "id": "wechat",
      "name": "微信支付",
      "icon": "url"
    },
    {
      "id": "card",
      "name": "银行卡",
      "icon": "url"
    }
  ]
}
```

#### 前端页面结构

**PaymentMethod.tsx**
```typescript
- 支付方式列表
- 支付方式卡片
- 选择按钮
- 确认支付按钮
```

---

### 4. 基础通知系统

#### 后端 API 设计

**端点 1: 获取通知列表**
```
GET /api/notifications
响应:
{
  "notifications": [
    {
      "id": "notif-1",
      "type": "order",
      "title": "订单已确认",
      "message": "您的订单已确认",
      "createdAt": 1702857600000,
      "read": false
    }
  ]
}
```

**端点 2: 标记为已读**
```
PUT /api/notifications/:id/read
响应:
{
  "success": true
}
```

#### 前端页面结构

**NotificationCenter.tsx**
```typescript
- 通知列表
- 通知卡片
- 标记已读按钮
- 删除通知按钮
- 通知筛选
```

---

## 🟠 P1 优先级功能

### 5. 手机号注册

#### 后端 API 设计

**端点: 发送短信验证码**
```
POST /api/auth/send-sms
请求体:
{
  "phone": "13800138000"
}
响应:
{
  "success": true,
  "message": "验证码已发送"
}
```

#### 前端页面结构

**Register.tsx 增强**
```typescript
- 邮箱/手机号切换
- 手机号输入框
- 验证码输入框
- 发送验证码按钮
- 倒计时显示
```

---

### 6. 库存告警系统

#### 后端 API 设计

**端点: 设置库存告警**
```
POST /api/inventory-alerts
请求体:
{
  "eventId": "event-1",
  "threshold": 50
}
响应:
{
  "success": true,
  "message": "告警已设置"
}
```

#### 前端页面结构

**InventoryAlert.tsx**
```typescript
- 活动列表
- 库存显示
- 告警阈值设置
- 告警历史
```

---

### 7. 用户管理后台

#### 后端 API 设计

**端点 1: 获取用户列表**
```
GET /api/admin/users
响应:
{
  "users": [
    {
      "id": "user-1",
      "username": "zhangsan",
      "email": "zhang@example.com",
      "status": "active"
    }
  ]
}
```

**端点 2: 封禁用户**
```
POST /api/admin/users/:id/ban
响应:
{
  "success": true
}
```

#### 前端页面结构

**UserManagement.tsx**
```typescript
- 用户列表表格
- 搜索功能
- 封禁/解禁按钮
- 导出用户数据按钮
- 用户详情弹窗
```

---

### 8. 支付记录页

#### 前端页面结构

**PaymentHistory.tsx**
```typescript
- 支付记录列表
- 支付方式显示
- 支付金额显示
- 支付状态显示
- 支付凭证下载
- 日期筛选
```

---

### 9. 退款申请流程

#### 后端 API 设计

**端点 1: 申请退款**
```
POST /api/refunds
请求体:
{
  "orderId": "order-1",
  "reason": "不想去了"
}
响应:
{
  "id": "refund-1",
  "status": "pending"
}
```

**端点 2: 审批退款**
```
POST /api/admin/refunds/:id/approve
响应:
{
  "success": true
}
```

#### 前端页面结构

**RefundRequest.tsx**
```typescript
- 订单选择
- 退款原因输入
- 退款金额显示
- 提交按钮
- 退款状态追踪
```

---

### 10. 社交登录

#### 后端 API 设计

**端点: OAuth 回调**
```
POST /api/auth/oauth-callback
请求体:
{
  "provider": "wechat",
  "code": "auth_code"
}
响应:
{
  "token": "jwt_token",
  "user": { ... }
}
```

#### 前端页面结构

**Login.tsx 增强**
```typescript
- 微信登录按钮
- QQ登录按钮
- 支付宝登录按钮
- 账号绑定提示
```

---

## 🟡 P2 优先级功能

### 11. 活动数据分析

#### 前端页面结构

**EventAnalytics.tsx**
```typescript
- 销售统计图表
- 收入统计图表
- 用户画像分析
- 座位热力图
- 时间段分析
```

---

### 12. 系统监控页

#### 前端页面结构

**SystemMonitor.tsx**
```typescript
- 响应时间图表
- 错误率图表
- 并发用户数
- 系统负载
- 实时告警
```

---

### 13. 权限管理系统

#### 前端页面结构

**PermissionManagement.tsx**
```typescript
- 角色列表
- 权限配置
- 用户角色分配
- 权限继承关系
```

---

## 📊 实现优先级矩阵

| 功能 | 复杂度 | 优先级 | 建议顺序 |
|------|--------|--------|---------|
| 忘记密码 | 低 | P0 | 1 |
| 活动创建 | 高 | P0 | 2 |
| 支付方式 | 中 | P0 | 3 |
| 通知系统 | 中 | P0 | 4 |
| 手机号注册 | 中 | P1 | 5 |
| 库存告警 | 中 | P1 | 6 |
| 用户管理 | 中 | P1 | 7 |
| 支付记录 | 低 | P1 | 8 |
| 退款申请 | 中 | P1 | 9 |
| 社交登录 | 高 | P1 | 10 |
| 数据分析 | 高 | P2 | 11 |
| 系统监控 | 高 | P2 | 12 |
| 权限管理 | 中 | P2 | 13 |

---

**指南版本**: 1.0
**最后更新**: 2025-12-16

